# Copy this file to .env for local development (defaults already work
# for localhost, so this is optional locally).
#
# On Render/Vercel/Netlify, set these as BUILD-TIME environment
# variables in the static site's settings, then redeploy:
VITE_API_URL=http://localhost:5000/api
VITE_SOCKET_URL=http://localhost:5000
